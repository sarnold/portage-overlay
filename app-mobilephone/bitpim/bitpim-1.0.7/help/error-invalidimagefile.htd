#include "pagestart.h"

SCREENSHOT(screen-invalidimagefile.png,)

<p>The named file is not a valid image and BitPim can't process it.
It is recommended that you delete the file.  You can also tell BitPim
to ignore the file, but you will be notified about it again whenever
BitPim rereads the contents of the directory.


#include "pageend.h"
